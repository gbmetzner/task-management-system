meta {
  name: Login-User
  type: http
  seq: 3
}

post {
  url: {{url}}/api/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john@example.com",
    "password":"password123"
  }
}

vars:post-response {
  auth-token: .*"token": .*
}

settings {
  encodeUrl: true
  timeout: 0
}
